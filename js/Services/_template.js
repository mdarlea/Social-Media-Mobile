(function () {
    'use strict';
    angular.module('app').factory('Service', ['$http', '$q', function ($http, $q) {

        var service = {
        
        };

        return service;
    }]);
})();